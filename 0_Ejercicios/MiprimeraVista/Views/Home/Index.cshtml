@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@

@{
}
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    Hello from the Index view
    <p>Uso de sentencias for en Razor</p>
    <div>
        @for (int i = 0; i < 5; i++)
        {
            <div> @i  @@</div>
        }
    </div>
    <p>Uso de etiquetas "text"</p>
    @for (int i = 0; i < 2; i++)
    {
        <text>
            i
            i
        </text>
    }
    <p>An implicit code expresion</p>
    <span>Price Including Sales Tax: @ViewBag.Price * 1.21</span>

    <p>Using parentheses to explicit delimit expression</p>
    <span>Price Including Sales Tax: @(ViewBag.Price * 1.21)</span>

    <p>A Razor code block</p>
    <div>
        @{
            int a = 5;
            int j = 6;
            int z = a + j;
            <div>@z</div>
        }
    </div>

    @{
        int b = 5;
        int w = 6;
        @if (b < w)
        {
            <p>@b</p>
        }
    }

    <p>A Razor forEach code block</p>
    @foreach (string name in ViewBag.Names)
    {
        <span>@name</span>
    }

    <p>Iterating a Model by using Razon</p>
    @model IEnumerable<MiprimeraVista.Controllers.Product>

    @foreach (var product in Model)
    {
        <span>@product.Name</span>
    }


</body>
</html>